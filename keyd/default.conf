[ids]
*
# Mouse input shares modifier state, preserving Super+drag and scroll.
m:03f0:0f8f
m:258a:0150
m:1209:0003

[main]
# Resolve the standalone gesture on release; intervening keys retain Super.
meta = timeout(overload(meta, f13), 400, overload(meta, f14))
